[project]
name = "shared"
version = "0.1.0"
description = "Shared abstractions for AD. Publish"
requires-python = ">=3.12"
dependencies = [
    "redis>=5.2.1",
    "pydantic>=2.10.6",
    "structlog>=24.1.0",
    "psycopg2-binary>=2.9.9",
]
