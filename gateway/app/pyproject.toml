[project]
name = "gateway"
version = "0.1.0"
description = "AD. Publish gateway"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ad-tech-inc-resilient-http>=0.2.2",
    "fastapi[standard]>=0.136.0",
    "redis>=8.0.1",
]
